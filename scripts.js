
/*function typeWriter(elemento) {
    const textoArray = elemento.innerHTML.split('');
    elemento.innerHTML = '';
    function atrasoInicial() {
        textoArray.forEach((letra, i) => {
            setTimeout(() => elemento.innerHTML += letra, 130 * i);
        });
    }
    const tempoDeEspera = 1700;
    setTimeout(atrasoInicial, tempoDeEspera);
}

const palavras = document.querySelector('#bio-name');
typeWriter(palavras);*/





